{
  "format_version": "1.8.0",
  "render_controllers": {
    "controller.render.hud_temperature": {
      "geometry": "geometry.hud_temperature",
      "materials": [ { "*": "Material.default" } ],
      "textures": [ "query.mark_variant == 1 && !query.is_onfire ? texture.temperature_freezing : (query.mark_variant == 2 && !query.is_onfire ? texture.temperature_cold : (query.mark_variant == 4 && !query.is_onfire ? texture.temperature_warm : (query.mark_variant == 5 && !query.is_onfire ? texture.temperature_burning : (query.mark_variant == 3 && !query.is_onfire ? texture.temperature_neutral : texture.temperature_burning))))" ],      
      "is_hurt_color": {},
      "on_fire_color": {},
      "light_color_multiplier": 1.3,
      "ignore_lighting":true
    }
  }
}
